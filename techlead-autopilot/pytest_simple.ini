[tool:pytest]
# Simple test configuration without complex dependencies
minversion = "7.0"
testpaths = ["tests/test_simple.py"]
addopts = [
    "-v",
    "--tb=short", 
    "--disable-warnings",
    "--no-cov",
    "--ignore=conftest.py"
]
python_paths = ["src"]