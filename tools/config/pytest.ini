[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function 
testpaths = tests

markers =
    integration: marks tests as integration tests (require external services like Memgraph)
    graph: relates to graph operations
    temporal: relates to temporal types or operations
    unit: marks tests as unit tests (self-contained) 